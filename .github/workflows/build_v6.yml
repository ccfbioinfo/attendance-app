name: Build Windows EXE v5.9.2

on:
  push:
    branches: [ main ]
  workflow_dispatch:   # 允许手动触发

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install pyinstaller
          pip install tkcalendar
          pip install openpyxl

      - name: Build EXE
        run: |
          #pyinstaller --onefile --windowed attendance.py
          pyinstaller --onefile --windowed --hidden-import=openpyxl attendance_v5.9.2.py

      - name: Upload EXE artifact
        uses: actions/upload-artifact@v4
        with:
          name: attendance-exe_v5.9.2
          path: dist/attendance_v5.9.2.exe
